# SQLite path (for local dev)
DATABASE_URL=sqlite+aiosqlite:////tmp/transactions.db


# For Postgres (if deploying)
# DATABASE_URL=postgresql+asyncpg://<user>:<pass>@<host>:<port>/<db>

# Optional: secret for idempotency header (not required)
WEBHOOK_SECRET=

# For deployment: the app host/port configuration
HOST=0.0.0.0
PORT=8000
